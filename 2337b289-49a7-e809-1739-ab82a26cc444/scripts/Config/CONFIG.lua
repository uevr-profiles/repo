-- Graphical Performance Settings 

-- Lumen Only Indoors Settings
	-- user_script.txt controls initial settings on inject, config values below used for dynamic adjustments
	lumen_onlyindoors = true -- Determines if lumen is enabled only when indoors
	shadowquality_outdoors = 1 -- Shadow Quality defines the shadow method which allows to adjust for quality or performance.
	shadowquality_indoors = 3 -- 0:off, 1:low(unfiltered), 2:low 3: med: 4: high 5:max

configInit=true
